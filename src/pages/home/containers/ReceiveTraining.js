import { connect } from 'react-redux';
import ReceiveTraining from '../components/ReceiveTraining';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(ReceiveTraining);
